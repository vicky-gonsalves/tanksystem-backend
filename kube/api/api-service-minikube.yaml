apiVersion: v1
kind: Service
metadata:
  name: api
  labels:
    run: api
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 9000
      protocol: TCP
      nodePort: 30000
  selector:
    run: api
